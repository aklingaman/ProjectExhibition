to compile: javac -cp ".:../lib/*" KNN.java Review.java OrderedReviewList.java 
	(This is for linux, the windows classpath argument is slightly different) 
to run: java -cp ".:../lib/*" KNN
	Again, for linux specifically. 

The easiest way to run it on windows is to use a smaller IDE like JGrasp or Dr. Java that wont require a package structure, but can use an external library just by showing it to the IDE. 